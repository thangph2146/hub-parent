dựa vào @users và dùng chung @resources  nếu thiếu utils thì bổ xung vào @resources  và dựa theo model từ @schema.prisma tạo page danh mục và feature theo chuẩn nextjs 16 @Nextjs Docs và @docs và đảm bảo không bug  và sử dụng zod để validated đảm bảo  tất cả action gửi notication realtime cùng với hiển thị thông báo socket.io @notifications chỉ xoá vĩnh viễn khi ở danh sách đã xoá